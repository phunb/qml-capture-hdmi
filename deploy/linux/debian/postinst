#!/bin/bash
set -e
export DEBIAN_FRONTEND="${DEBIAN_FRONTEND:-noninteractive}"
export HDMI_KIOSK_ROOT="${HDMI_KIOSK_ROOT:-/opt/hdmi-kiosk}"
export KIOSK_USER="${KIOSK_USER:-kiosk}"
bash "${HDMI_KIOSK_ROOT}/share/setup-kiosk-mode.sh"
echo "Cài xong HDMI Kiosk. Khởi động lại máy: sudo reboot"
